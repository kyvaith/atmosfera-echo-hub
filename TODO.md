# Atmosfera Echo Hub TODO

- Revisit the manual direct framebuffer snapshot compositor for tile swipes after the broader LVGL performance work. The target baseline is the version after the excessive touch-handler work was removed, but before the manual direct compositor was disabled: swipe UX was good there, and that exact combination is worth restoring once LVGL/render throughput is improved.
- Look for a high-quality LVGL swipe/pager reference implementation and compare it against the current home-screen swipe behavior. The current gesture still does not feel natural enough, especially around thresholding, inertia, and the final settle animation.
- Test keeping LVGL swipe/page snapshots in PSRAM as compressed JPEG payloads, using ESP32-P4 hardware JPEG encode/decode where possible. Compare memory pressure, decode latency, cache-sync cost, and visual artifacts against raw RGB888 panorama snapshots before deciding whether compressed snapshot storage is worth the complexity.
- Evaluate replacing the custom home-screen swipe pager with ESPHome/LVGL `tileview`: keep one-page-per-swipe behavior, preserve the up-swipe app-close gesture, and compare invalidation/FPS against the current manual `controls_root_*` positioning before adopting it.
- Experiment with a generic LVGL virtualized/recycled list mechanism: keep only the visible row widgets alive and update their content/position from a backing data model while scrolling. This must be designed as a reusable collection primitive for settings, menus, pickers, and future long lists, not as a one-off settings-page workaround.
- ~~Add explicit allowed character sets for UI fonts/text rendering so generated fonts include every symbol, localized character, icon, and dynamic text variant the interface can display.~~
- ~~Harden the full LVGL compile-time profiler path (`profiler: true`) so it is reliable on-device: prevent boot loops/panics, keep a reproducible settings-scroll capture flow, and document when to use compile-time profiling versus the lightweight runtime perf logger.~~
- Fix minimum and maximum display brightness limits: brightness below roughly 70% currently turns the screen fully off. Measure the real usable PWM range for this panel/backlight circuit, then set safe min/max thresholds and remap the UI brightness slider so low values remain visible while full brightness stays within the panel's comfortable range.
- Investigate the remaining occasional blue screen flash that appears every few minutes. Check for rare DSI underruns, bandwidth spikes, and timing-margin issues; test whether lowering display bandwidth/lane bitrate or pixel clock improves stability without hurting responsiveness.
- Add UI localization support with a clear supported-language matrix. Define the initial language set, font coverage, date/time formatting, pluralization strategy, Home Assistant language integration, and fallback behavior for missing translations.
- Add theme color customization: provide polished preset palettes and allow users to select a custom base/accent color, then derive the Material-style surface, primary, secondary, outline, and state colors consistently across the UI.
- Add a device information settings page with live diagnostics such as IP address, Wi-Fi state/RSSI, uptime, firmware version/channel, CPU frequency/load, RAM/PSRAM usage, flash size, display mode, and key runtime flags.
- Promote screenshot/snapshot mode to a reusable `page`-level parameter in the LVGL YAML structure instead of keeping it as page-specific custom logic.
- Add a configurable screenshot refresh cadence for snapshot-backed pages. Generate the next snapshot in the background and only replace the old snapshot after the new one is fully rendered, so a swipe that starts mid-refresh can still animate smoothly without a blank or stalled frame.
- Investigate SendSpin playback interruptions where music stops or glitches with `sendspin.time_burst: Time message 1/8 timed out`. Check Wi-Fi stability, socket pressure, timing bursts, buffering, and whether `CONFIG_LWIP_MAX_SOCKETS` or network task scheduling contributes.
- Investigate why `CONFIG_LWIP_MAX_SOCKETS` is currently set to 12 while ESPHome reports that this configuration needs 18 sockets. Decide whether to raise the limit, reduce open socket users, or tune API/web/SendSpin networking so socket exhaustion cannot interrupt audio or OTA.
- Investigate the startup click/pop when music playback begins. Review the source media-player project for any workaround such as delayed amplifier enable, DAC mute/unmute sequencing, fade-in, or I2S warm-up before enabling the PA.
- Remove the `CORE.using_esp_idf` deprecation warning by updating custom/external components to use `CORE.is_esp32` and/or `CORE.using_arduino` as recommended by ESPHome.
- Fix the ESPHome `transparency_key` config warning: `Field 'transparency_key' is being redacted by a legacy substring heuristic`. Update the LVGL schema validator to mark the field with `cv.sensitive(...)` or otherwise follow ESPHome's recommended redaction path before the heuristic is removed in 2026.12.0.
- Explore backdrop blur support for LVGL overlays, including whether LVGL 9.5 can do this efficiently on ESP32-P4 or whether a snapshot-based blur/cache is required.
- Introduce global reusable style definitions for common UI primitives so buttons, tiles, rows, overlays, labels, and controls are not styled independently in every page.
- Compare LVGL Lottie integration paths: `libs/rlottie` examples versus the native `widgets/lottie` examples. Decide which path ESPHome should expose or optimize, and document the tradeoffs for our use case.
- Investigate `touchscreen took a long time for an operation` warnings, especially spikes around 262 ms. Profile touch polling, I2C timing, gesture handling, LVGL input processing, and any blocking callbacks.
- Remove or suppress known SendSpin warnings that only appear with `-O3`, after confirming they are harmless and not hiding real audio timing problems.
- Add a local alarm clock feature with on-device scheduling, Home Assistant synchronization, wake/snooze controls, persistent alarm state, and audio/visual/haptic feedback.
- Add Home Assistant notification support with a dedicated notification model, queue/history, priorities, visual overlays, actionable buttons where possible, and sensible interruption rules for music/voice sessions.
- ~~Evaluate an ESP32-P4 bridge for LVGL's DMA2D draw backend. The upstream LVGL DMA2D backend is currently STM32-register based, so enabling it on this hardware needs a real ESP-IDF/PPA/DMA2D HAL adapter rather than only unfiltering `src/draw/dma2d`.~~
- ~~Revisit dual-core display flushing as a driver-level experiment: keep LVGL rendering into small internal-RAM partial buffers while a separate task/core copies into the inactive full MIPI framebuffer, swaps at frame end, and syncs the idle buffer. The first LVGL-component prototype bootlooped before logger initialization, so the next attempt should be gated behind the MIPI DSI driver and introduced in smaller link/runtime steps.~~
- Port the remaining useful youkorr LVGL 9.5 work into our ESPHome fork while preserving attribution where possible: additional widgets (`arclabel`, `calendar`, `chart`, `imgbtn`, `list`, `menu`, `scale`, `span`, `table`, `win`), extensions to existing widgets (`arc`, `dropdown`, `keyboard`, `meter`, `msgbox`, `qrcode`, `slider`, `tabview`), expanded canvas actions (begin/end draw, invalidate, copy buffer, palette, get image), style transitions in `widgets/__init__.py`, and the larger validation/schema/automation/layout/lvcode improvements.
